# dotfiles

The essential collection for configuring my mac's development environment.

Remember to clone under `~/.dotfiles` folder path.

## Instructions

```BASH
cd ~/.dotfiles
./setup_mac
rcup
```
